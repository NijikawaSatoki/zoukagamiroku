/* The way I'm gonna do namespaces for functions and global variables is like 
 * this: namespace_Name
 * Hopefully that makes sense >_<'
 * 
 * <3 Satoki Nijikawa (Niji System)
 */

// =============================================================================
// Hello World Example :3
// =============================================================================

// Prints `Hello World!' in an alert box
function index_HelloWorld() {
    let hello = "Hello World!";
    window.alert(hello);
    return 0;
}
